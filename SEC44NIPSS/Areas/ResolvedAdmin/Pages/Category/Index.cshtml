@page
@model SEC44NIPSS.Areas.ResolvedAdmin.Pages.Category.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DocumentCategory[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DocumentCategory[0].ShortDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DocumentCategory[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DocumentCategory[0].Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DocumentCategory[0].Alumni)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DocumentCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alumni.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
