@page
@model SEC44NIPSS.Areas.Admin.Pages.Team.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               @* @Html.DisplayNameFor(model => model.Executive[0].Fullname)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Executive[0].Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Executive[0].SortOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Executive[0].Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Executive[0].Manifesto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Executive) {
        <tr>
            <td>
              @*  @Html.DisplayFor(modelItem => item.Fullname)*@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SortOrder)
            </td>
            <td>
                <img src="@item.Image" style="max-height:100px;" />

            </td>
            <td>
                @Html.Raw(item.Manifesto)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
