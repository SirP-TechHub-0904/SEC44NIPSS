@page
@model SEC44NIPSS.Areas.Admin.Pages.Maintainance.Setting.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaintainaceSetting[0].FirstResponceTimeAfterSubmit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaintainaceSetting[0].SecondResponceTimeAfterApproval)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MaintainaceSetting) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstResponceTimeAfterSubmit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SecondResponceTimeAfterApproval)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
